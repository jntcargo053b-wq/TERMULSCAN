name: Build TERMULScan APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # ========== PERBAIKAN 1: Pastikan nama package = termulscan ==========
      - name: Fix package name in pubspec.yaml
        run: |
          if [ -f pubspec.yaml ]; then
            sed -i 's/^name: *.*/name: termulscan/' pubspec.yaml
            echo "✅ nama package sudah diperbaiki:"
            grep '^name:' pubspec.yaml
          fi

      # ========== PERBAIKAN 2: Tambahkan SDK constraint jika belum ada ==========
      - name: Add SDK constraint to pubspec.yaml
        run: |
          if ! grep -q 'sdk: *' pubspec.yaml; then
            echo "Menambahkan SDK constraint..."
            sed -i '/^environment:/a \  sdk: ">=3.0.0 <4.0.0"' pubspec.yaml
          else
            # Jika sudah ada tapi tidak punya batas bawah, perbaiki
            sed -i 's/^  sdk:.*$/  sdk: ">=3.0.0 <4.0.0"/' pubspec.yaml
          fi
          echo "✅ SDK constraint sekarang:"
          grep -A1 '^environment:' pubspec.yaml

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          cache: true

      - name: Write local.properties
        run: |
          FLUTTER_SDK=$(dirname $(dirname $(which flutter)))
          echo "flutter.sdk=$FLUTTER_SDK" > android/local.properties
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> android/local.properties

      - name: Generate gradlew (with forced project name)
        run: |
          # Paksa nama proyek = termulscan agar tidak error
          flutter create --platforms android --project-name termulscan .
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Kembalikan main.dart asli (jika ada)
          git checkout -- lib/main.dart || true
          git add -f android/gradlew android/gradlew.bat android/gradle/wrapper/gradle-wrapper.jar
          git diff --staged --quiet || git commit -m "ci: add gradlew [skip ci]"
          git stash
          git pull --rebase origin main
          git stash pop || true
          git push || true

      - name: Fix Gradle permission
        run: chmod +x android/gradlew

      # ========== PENTING: Pastikan file MainActivity.kt Anda tetap utuh ==========
      - name: Restore custom MainActivity.kt (jika ada)
        run: |
          # Jika Anda sudah punya MainActivity.kt di repo, step ini memastikan tidak tertimpa
          if [ -f android/app/src/main/kotlin/com/termulscan/app/MainActivity.kt ]; then
            echo "✅ MainActivity.kt sudah ada, tidak akan ditimpa oleh flutter create"
          else
            echo "⚠️ MainActivity.kt tidak ditemukan, membuat default"
            mkdir -p android/app/src/main/kotlin/com/termulscan/app
            cat > android/app/src/main/kotlin/com/termulscan/app/MainActivity.kt << 'EOF'
package com.termulscan.app

import android.Manifest
import android.content.pm.PackageManager
import android.location.Location
import android.location.LocationListener
import android.location.LocationManager
import android.os.Bundle
import android.os.Handler
import android.os.Looper
import androidx.core.app.ActivityCompat
import androidx.core.content.ContextCompat
import io.flutter.embedding.android.FlutterActivity
import io.flutter.embedding.engine.FlutterEngine
import io.flutter.plugin.common.MethodChannel
import io.flutter.plugin.common.MethodChannel.Result

class MainActivity : FlutterActivity() {
    private val CHANNEL = "com.termulscan.app/location"
    private lateinit var locationManager: LocationManager

    override fun configureFlutterEngine(flutterEngine: FlutterEngine) {
        super.configureFlutterEngine(flutterEngine)
        locationManager = getSystemService(LOCATION_SERVICE) as LocationManager

        MethodChannel(flutterEngine.dartExecutor.binaryMessenger, CHANNEL)
            .setMethodCallHandler { call, result ->
                if (call.method == "getLocation") {
                    getCurrentLocation(result)
                } else {
                    result.notImplemented()
                }
            }
    }

    private fun getCurrentLocation(result: Result) {
        // Cek permission
        val hasFineLocation = ContextCompat.checkSelfPermission(
            this, Manifest.permission.ACCESS_FINE_LOCATION
        ) == PackageManager.PERMISSION_GRANTED
        val hasCoarseLocation = ContextCompat.checkSelfPermission(
            this, Manifest.permission.ACCESS_COARSE_LOCATION
        ) == PackageManager.PERMISSION_GRANTED

        if (!hasFineLocation && !hasCoarseLocation) {
            result.error("PERMISSION_DENIED", "Location permission not granted", null)
            return
        }

        // 1. Cek last known location (sangat cepat)
        var bestLocation: Location? = null
        val gpsLast = locationManager.getLastKnownLocation(LocationManager.GPS_PROVIDER)
        val netLast = locationManager.getLastKnownLocation(LocationManager.NETWORK_PROVIDER)

        bestLocation = gpsLast ?: netLast

        if (bestLocation != null) {
            val map = mapOf(
                "lat" to bestLocation.latitude,
                "lng" to bestLocation.longitude,
                "accuracy" to bestLocation.accuracy
            )
            result.success(map)
            return
        }

        // 2. Tidak ada cache – request single update dari semua provider yang aktif
        val providers = listOf(
            LocationManager.GPS_PROVIDER,
            LocationManager.NETWORK_PROVIDER,
            LocationManager.PASSIVE_PROVIDER
        )
        var requested = false
        var resultSent = false

        val locationListener = object : LocationListener {
            override fun onLocationChanged(location: Location) {
                if (!resultSent) {
                    resultSent = true
                    val map = mapOf(
                        "lat" to location.latitude,
                        "lng" to location.longitude,
                        "accuracy" to location.accuracy
                    )
                    result.success(map)
                }
                // Hapus listener setelah berhasil
                providers.forEach { provider ->
                    try { locationManager.removeUpdates(this) } catch (_: Exception) {}
                }
            }

            override fun onStatusChanged(provider: String?, status: Int, extras: Bundle?) {}
            override fun onProviderEnabled(provider: String) {}
            override fun onProviderDisabled(provider: String) {}
        }

        for (provider in providers) {
            if (locationManager.isProviderEnabled(provider)) {
                try {
                    locationManager.requestSingleUpdate(provider, locationListener, Looper.getMainLooper())
                    requested = true
                } catch (_: Exception) {
                    // Provider mungkin tidak support requestSingleUpdate, abaikan
                }
            }
        }

        if (!requested) {
            result.error("NO_PROVIDER", "No location provider enabled", null)
            return
        }

        // 3. Timeout 5 detik – jika tidak ada lokasi, kirim null
        Handler(Looper.getMainLooper()).postDelayed({
            if (!resultSent) {
                resultSent = true
                result.success(mapOf(
                    "lat" to null,
                    "lng" to null,
                    "accuracy" to null
                ))
                // Bersihkan listener
                providers.forEach { provider ->
                    try { locationManager.removeUpdates(locationListener) } catch (_: Exception) {}
                }
            }
        }, 5000)
    }
}
EOF
          fi

      - name: Add url_launcher queries to AndroidManifest
        run: |
          MANIFEST=android/app/src/main/AndroidManifest.xml
          if ! grep -q 'android.intent.action.VIEW' "$MANIFEST"; then
            sed -i 's|</manifest>|<queries>\n  <intent>\n    <action android:name="android.intent.action.VIEW" />\n    <data android:scheme="geo" />\n  </intent>\n  <intent>\n    <action android:name="android.intent.action.VIEW" />\n    <data android:scheme="https" />\n  </intent>\n</queries>\n</manifest>|' "$MANIFEST"
          fi

      - name: Generate launcher icons
        run: |
          pip install pillow -q
          python3 -c "
          from PIL import Image
          import os
          sizes = {
            'mipmap-mdpi': 48,
            'mipmap-hdpi': 72,
            'mipmap-xhdpi': 96,
            'mipmap-xxhdpi': 144,
            'mipmap-xxxhdpi': 192
          }
          for folder, size in sizes.items():
            path = f'android/app/src/main/res/{folder}'
            os.makedirs(path, exist_ok=True)
            img = Image.new('RGBA', (size, size), (0, 230, 118, 255))
            img.save(f'{path}/ic_launcher.png')
            img.save(f'{path}/ic_launcher_round.png')
          print('Icons generated')
          "

      - name: Get dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Rename APK
        run: |
          mkdir -p output
          cp build/app/outputs/flutter-apk/app-release.apk output/TERMULScan-release.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TERMULScan-apk
          path: output/TERMULScan-release.apk
